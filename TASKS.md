# H.265 V1.4 开发任务

最后更新：2026-07-27

## 当前状态

v1.4.0 当前正式入口输出 x265 原生默认、通用无 ROI、预算中性 ROI、ROI + 降噪实验项四路结果。通用无 ROI 是 ROI 预算基准：复用 V1.0 原激进帧间结构、`medium` preset、AQ2，不使用静态 ROI QP 或 ROI 分区降噪。ROI 两路必须在通用无 ROI 平均视频包码率预算内重新分配码率，并保证 critical/evidence 重点区域局部 VMAF、P5、SSIM 不低于通用无 ROI 方案；超过预算或局部质量下降时标记失败，不发布对应 ROI 输出。三个非默认策略统一使用 VMAF≥83、P5≥80、SSIM≥0.950，分别独立搜索 CRF，不做 CRF 配对，不评选胜出方案，也不生成部署结论。真实 60 秒素材回归已完成：默认 0.568393 Mbit/s；通用无 ROI CRF36.5、0.206501 Mbit/s、相对默认节省63.67%；预算中性 ROI CRF37.5、尝试0.274063 Mbit/s、相对通用节省-32.72%、预算失败；ROI + 降噪 CRF38.0、尝试0.255483 Mbit/s、相对通用节省-23.72%、预算失败；两个 ROI 输出均未发布。

## 已完成

- [x] 清空旧版项目并在原路径建立全新 `hevc_lab`。
- [x] 恢复项目内 FFmpeg、FFprobe 和 VMAF 工具链。
- [x] 实现工具链检查和视频信息探测。
- [x] 实现固定 CRF 的多组帧间参数编码原型。
- [x] 实现 VMAF 平均值、VMAF P5、SSIM和视频流码率统计。
- [x] 实现 CSV、JSON、Markdown报告。
- [x] 实现算法基线收益和相对源流部署收益分离。
- [x] 实现负收益原流直通保护。
- [x] 完成合成样本和真实监控12秒片段验证。
- [x] V1.3 阶段已有113项单元测试、环境检查和真实60秒四路回归通过。
- [x] 保存项目共享上下文和 V1.0 正式设计。
- [x] 实现 V1.1 保守/均衡目标节省区间搜索、报告字段、Web 展示和 60 秒回归。
- [x] 实现 V1.2 默认 + 激进+ / 激进++ / 激进+++ 四路输出、模式配置、报告字段、Web 展示和测试覆盖。
- [x] 实现 V1.3 默认 / 保守 / 综合 / 激进四路重构：保守通用 `medium` 且无 ROI/降噪，综合映射 V1.0 原激进，激进映射 V1.2 激进+，报告和 Web 改为公开策略描述驱动。
- [x] 实现 V1.4 预算中性 ROI 迭代：默认 / 通用无 ROI / 预算中性 ROI / ROI + 降噪实验项四路输出，ROI 两路受通用方案平均视频包码率预算约束，并复算重点区域局部画质；失败不发布 ROI 输出。
- [x] 现有115项单元测试、环境检查和 V1.4 真实60秒四路回归通过；已生成的默认与通用无 ROI MP4 完整解码退出码均为0。
- [x] 新增 `apps/demo` Cloudflare Pages 静态展示版：原生 HTML/CSS/JS，从 `data/results.json` 读取离线四路结果，使用 `videos/previews` 的 H.264 文件做同步对比预览，使用 `videos/hevc` 提供 H.265/HEVC 下载，不包含上传、FastAPI、Python、FFmpeg 或任务队列依赖。

## V1.0 待开发任务

按以下顺序推进；新对话从第一项未完成任务继续。

- [x] 1. 重整模块目录，分离算法核心、编码执行、指标、输入适配、报告和网页；保持现有测试可运行。
- [x] 2. 实现统一参考片段准备：开始时间、时长、时间戳归零、FFV1无损缓存、帧数校验和输入哈希。
- [x] 3. 固化两套参数模型：工程基线H.265和帧间优化组合H.265；确保除帧间参数外其他条件一致。
- [x] 3A. 实现保守、综合、激进三种帕累托模式；统一管理优化参数、质量门槛、算法/源流节省门槛，并接入命令行和报告。
- [x] 4. 实现单方案 CRF 18～38、0.5精度的自适应画质搜索及相邻点复核。
- [x] 5. 实现两套方案独立搜索和肉眼无损候选配对；两路先分别通过模式画质门槛，无法满足VMAF差绝对值≤1.0时返回证据不足。
- [x] 5A. 实现质量驱动 Capped CRF/VBV 研究：以自然 CRF 码率派生峰值上限，画质失败自动放宽，并按三模式复用策略。
- [x] 5B. 实现 AQ 自适应量化研究：以默认 AQ2 为对照，让暗场 AQ3 和边缘 AQ4 独立搜索、等画质配对，失败回退，并按三模式复用强度和空间粒度。
- [x] 5C. 实现固定机位静态 ROI 研究：配置校验、三模式 QP 策略、`addroi` 编码、独立 CRF 搜索、重点区域指标、无收益回退和可视化报告。
- [x] 5D. 实现 ROI 保护的无效噪声抑制研究：三模式 `hqdn3d` 区域强度、无降噪/降噪独立 CRF 搜索、critical/evidence 局部画质保护、缓存隔离和无收益回退。
- [x] 5E. 将 x265 `slow` 集成到激进模式：保守/综合保持 `medium + 0.97x`，激进改为 `slow` 且不设速度硬门槛；完成12秒、60秒 medium/slow 独立研究和正式 aggressive 回归。
- [x] 5F. 实现 v1.0.0 四路独立输出：原生 x265 默认方案与保守、均衡、激进三档综合策略；组合 AQ2、ROI 保护降噪和静态 ROI QP，三档独立搜索 CRF，输出分辨率、平均视频包码率和相对默认节省百分比。
- [x] 5G. 实现 v1.2.0 激进三档扩展：默认方案对比激进+、激进++、激进+++，三档均使用 slow preset，分别使用 CRF 上限42/45/48、12/15/20秒 GOP、更强背景 ROI QP 和 ROI 保护降噪；报告输出 CRF、VMAF/P5/SSIM、编码速度和相对默认节省百分比。
- [x] 5H. 实现 v1.3.0 默认/保守/综合/激进四路重构：默认仍为 x265 原生默认；保守为 V1.0 原激进帧间结构的通用 medium 版且不读 ROI、不生成 `addroi`/`hqdn3d`；综合映射 V1.0 原激进；激进映射 V1.2 激进+；manifest 记录公开模式、内部来源、区域处理开关、最终 preset 和 CRF 搜索上限。
- [x] 6. 统一视频流包字节码率口径，并验证音频和容器开销不会进入核心比较。
- [x] 7. 实现算法可行性、软件画面连续性和部署可行性三种独立结论；连续处理速度门槛为0.97x，1.0x以上额外标记当前电脑可实时处理。
- [x] 8. 新增 `python -m hevc_lab compare` 命令和可恢复实验缓存：请求哈希、原子状态文件、阶段/尝试记录、失败恢复、候选缓存复用和完整结果短路命中。
- [x] 9. 重写CSV、JSON和Markdown报告：展示四路实际分辨率、平均视频包码率和三档相对默认方案节省百分比；内部清单保存CRF、画质、速度和完整参数。
- [x] 10. 实现本地网页文件上传、任务进度、结果卡片和失败原因展示。
- [x] 11. 生成不参与指标的H.264浏览器预览，并实现默认/所选综合策略的重叠式同步播放对比。
- [x] 12. 补充搜索、质量匹配、缓存、码率口径、结论判断、中文路径和异常处理测试，并新增 Web API、上传校验、队列、结果解析和预览命令测试。
- [ ] 13. 使用合成静态/运动样本完成端到端验收。
- [x] 14. 使用真实固定监控片段完成 v1.0.0 四路60秒端到端验收，并将事实结果写入 `PROJECT_CONTEXT.md`。
- [x] 15. 更新README和 `start_web.cmd`，确认新用户仅按文档即可运行。
- [x] 16. 拆分本地完整功能版与云端静态展示版；实现 `apps/demo` 静态目录和 Cloudflare Pages 配置说明。
- [x] 17. 实现本地 RTSP 实时预览第一版：FastAPI 启动 FFmpeg 拉流并输出 HLS，前端使用本地 `hls.js` 播放；本地首页挂载 `apps/demo_live`，视觉复用 `apps/demo` 的居中卡片、通用/专用 Tab、双视频重叠画面和拖动分割线；`apps/demo` 保持 Cloudflare Pages 纯静态展示版；该预览不参与 H.265 指标和部署结论。
- [x] 18. 实现 RTSP 实时双画面对比：同一 stream session 输出 `source` 与 `conservative` 双 HLS 预览，页面展示流状态、源流信息、HLS 状态和 FFmpeg 错误摘要，不展示 VMAF 或 CRF。
- [x] 19. 修正实时对比口径为两路摄像头侧实际编码流：`source` 输入原生 H.264 RTSP，`conservative` 输入摄像头侧 H.265 保守策略 RTSP；后端单独以 copy 计量链估算两路输入码率，H.264 HLS 只作为浏览器预览，不参与带宽节省计算；实时预览模块不重写保守策略参数。

## V1.4 完成后再做

- [ ] 对通用 / ROI / ROI + 降噪做单参数消融，拆分 ROI QP、降噪、GOP 和 lookahead 各自贡献。
- [ ] V1.5 率失真曲线和 BD-Rate。
- [ ] 调研真实摄像头支持的GOP、参考帧、B帧、lookahead和智能编码接口。
- [ ] 设计摄像头控制适配层与网关运行层。
- [ ] 将 RTSP 实时预览扩展为网关运行层原型，补充断流重连、长期监控、低延迟协议和实时编码策略验证。

## 当前禁止事项

- 不恢复旧版 V2/V2.3 网页、MediaMTX、WebRTC 或动态码率架构；当前 RTSP 能力仅限本地 FastAPI 拉流转 HLS 预览。
- 不使用手工目标码率比例伪造结果。
- 不把三档非默认策略做 CRF 配对或 VMAF/SSIM 差值配对。
- ROI 候选超过通用无 ROI 码率预算或重点区域局部质量下降时，不得发布 ROI 输出或伪造成收益。
- 不评选胜出方案，不把软件 libx265 结果表述成摄像头实机部署结论。
- 不把软件 libx265 结果表述成摄像头侧实机结论。
- 未通过源流节省门槛时不生成可部署优化视频。
