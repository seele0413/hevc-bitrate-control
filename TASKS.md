# V2.2.1 Remote Stable 第一阶段任务

最后更新：2026-08-03

## 已完成

- [x] 从 `V2.2` 标签提交 `0458025` 以 `git clone --no-hardlinks` 创建独立副本仓库。
- [x] 创建分支 `codex/v2.2.1-remote-stable`，仅配置 fetch 用 `upstream` 并禁用 push URL。
- [x] 复制项目内 `.tools/ffmpeg`，不复制历史 `work/`、`output/` 和浏览器测试产物。
- [x] 统一包、管线、CLI、网页和启动脚本版本为 `2.2.1`。
- [x] 保持源码 `-c:v copy`、正式 H.265 参数和 elementary stream 码率证据不变。
- [x] 增加唯一右路浏览器预览配置：ultrafast、CRF 26、3M/6M VBV、源分辨率、1 秒 GOP/HLS。
- [x] 后端按播放列表最新约 30 秒已封口 TS 统计 HLS 传输字节、媒体时长和实际传输码率。
- [x] 将前端改为源码 10 秒、H.265 预览 15 秒的独立固定延迟，不强制同帧。
- [x] 使用每一路自己的 `video.buffered`；低于 1.5 秒只暂停该路，在当前目标恢复 8 秒缓冲后再播放。
- [x] 恢复过程锁定目标，避免因 live edge 持续移动而反复起停。
- [x] 保留手动双路控制、停止/关闭统一回收和分割线拖动冻结双路行为。
- [x] 页面增加两路 HLS 边缘延迟、实际缓冲、下载速度、带宽余量、卡顿次数和恢复状态。
- [x] 带宽余量按 `<1.0x`、`1.0-1.3x`、`>=1.3x` 显示不足、偏紧和充足。
- [x] 修正 hls.js 1.5.17 分片统计读取路径，按 `data.frag.stats` 更新最近 10 个完整分片下载速度。
- [x] HLS 响应改为不可变字节快照，消除播放列表长度竞态和停止清理期间的文件打开竞态。
- [x] 子进程回收顺序改为先 terminate/wait/kill、后关闭管道，消除高背压下 stdin close/flush 阻塞。
- [x] 更新 AGENTS、共享上下文、设计、任务和 README 为当前方案。

## 验证状态

- [x] 前端 JavaScript 语法检查通过。
- [x] Python 编译检查通过。
- [x] 现有真实 FFmpeg 合成流集成测试通过。
- [x] 常规测试发现 23 项：22 项通过、120 秒长测默认跳过；长测已按环境变量显式执行通过。
- [x] `check-env` 环境检查通过。
- [x] 1080p/20fps 五进程管线运行 129.45 秒；源码 `0.756 Mbps`、右路 `3.089 Mbps`、队列 `0/100`，停止后五进程全部回收。
- [x] Playwright 桌面 1440x1000 与移动 390x844 无重叠、溢出、文字裁切，正常启动/停止控制台无错误。
- [x] Playwright 在 80 ms RTT、5 Mbps 下完成 120 秒稳定窗口，启动后两路卡顿增量均为 0、采样期间无暂停。
- [x] 单独中断右路 TS 时源码继续播放；低缓冲注入只暂停对应路线，缓冲恢复到高水位后继续。
- [x] 手动播放/暂停同时控制两路，分割线拖动期间双路冻结，结束后恢复。
- [x] 全仓版本、旧共同时间轴、旧恢复字段、旧预览参数和降噪残留扫描通过。

## 第一阶段边界

- 不处理疑似“声明 20 fps、实际约 15 fps”的时间基准问题。
- 不保留 RTSP 原始 PTS，不重构共享阻塞背压管线。
- 不增加降噪，不修改正式 H.265 编码方案和码率节省证据。
- 本阶段不提交、不打 tag、不推送 GitHub。
- 浏览器长测观察到现有媒体时间基准会让 HLS 边缘延迟从目标值缓慢漂移；按第一阶段边界留待后续处理。
