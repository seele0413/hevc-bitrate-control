# V2.2.0 固定延迟实时源码直流任务

最后更新：2026-07-30

## 已完成

- [x] 统一包、CLI、运行时、网页和启动脚本版本为 `2.2.0`。
- [x] CLI 收敛为 `check-env` 与 `web`。
- [x] 删除离线任务、历史策略、上传、指标报告和旧页面代码。
- [x] 工具链收敛为 FFmpeg 与 FFprobe，环境检查只验证实时链路需要的能力。
- [x] 建立唯一 H.265 固定配置，并由命令与状态共同读取。
- [x] RTSP 探测只接受 H.264 视频。
- [x] 使用单一 RTSP 连接输出 H.264 elementary stream，并在一次计数后原样分发。
- [x] 源码 HLS 使用 `stream copy`，不使用预览编码器。
- [x] 原始帧只进入 H.265 编码器，H.265 字节在仅观看预览前统计。
- [x] 保留阻塞背压、512 MiB/约 5 秒队列、单会话、心跳和统一回收。
- [x] 增加源码长 GOP 启动告警和 RTSP 全字段脱敏。
- [x] 更新公开 API 与网页术语，负节省显示为码率增加。
- [x] 重建实时单元测试与真实 FFmpeg 合成流集成测试。
- [x] 将网页重构为视频优先的实时操作台，保留原视频画布并重排连接、控制、参数与遥测区域。
- [x] 固定参数卡片由 `/api/runtime` 的唯一 H.265 配置动态生成。
- [x] 对比分割线改用 Pointer Events、pointer capture 与动画帧提交；拖动期间冻结双路播放并暂停同步调速。
- [x] 停止操作先立即卸载两路播放器，再等待后端统一回收进程，期间锁定会话控件并显示停止状态。
- [x] 双路共同时间轴达到约 10 秒后，从共同直播边缘之前 10 秒处起播并持续维持固定延迟。
- [x] 任一路播放卡顿时双路同步暂停；两路在原位置恢复至少 3 秒实际缓冲后再继续。
- [x] 只有原播放位置离开保留窗口时，才双路同步回到固定延迟位置，不允许单路静默跳到直播边缘。
- [x] 两路 HLS 保留窗口扩展为 60 个分片，播放列表禁止缓存和代理缓冲。
- [x] 页面租约放宽为 45 秒，心跳、状态查询和 HLS 文件请求均可续租。
- [x] 右路 H.264 仅观看预览调整为 `libx264 ultrafast / CRF 21`，正式 H.265 固定参数与码率统计保持不变。
- [x] 编码健康改为约 30 秒积压趋势，主状态显示“采样中 / 实时稳定 / 积压增长”，`encode_speed_x` 降为辅助处理节奏。

## 验证状态

- [x] 聚焦单元测试通过。
- [x] 合成 H.264 elementary stream 真实 FFmpeg 集成实验通过，两路 HLS 分片均可完整解码。
- [x] Python 全量编译检查通过。
- [x] 全部 19 项保留测试通过，并以 `ResourceWarning` 作为错误复核管道回收。
- [x] `check-env` 通过。
- [x] 前端 JavaScript 语法检查通过。
- [x] Playwright 桌面 1440x1000 与移动 390x844 页面检查通过；两路视频均完整加载并持续同步播放，无横向溢出、重叠或控制台错误。
- [x] Playwright 验证负节省显示为“码率增加”。
- [x] Playwright 在 1440x1000 与 390x844 复核新页面无横向溢出或控制台错误；分割线可稳定拖到 75.4% 并正确释放捕获。
- [x] Playwright 延迟后端停止响应时确认画面立即清空、状态切换为“停止中”且会话控件锁定。
- [x] Playwright 验证双路达到 10 秒共同时间轴后起播、固定延迟控制和 3 秒共同缓冲恢复。
- [x] Playwright 验证短暂卡顿从原位置继续，原位置过期时双路同时回到“共同直播边缘 - 10 秒”。
- [x] Playwright 验证固定积压显示“实时稳定”，持续约 `+0.6 s/min` 增长显示“积压增长”，`0.99x` 只作为辅助处理节奏。
- [x] Playwright 最终复核 1440×1000 与 390×844 页面无横向溢出、文字裁切、元素重叠或控制台错误。
- [x] 全仓已删除能力与旧接口残留扫描通过。
- [x] 验证结束后无合成服务或 FFmpeg 残留进程。

## 运行边界

- 当前没有真实 RTSP 凭据，因此合成流集成实验替代外部摄像头连续运行检查。
- `work/` 中已有用户结果未删除，V2.2.0 不读取这些旧结果。
