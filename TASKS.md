# V2.3.0 固定轻度噪声抑制任务

最后更新：2026-07-31

## 已完成

- [x] 统一包、CLI、运行时、网页和启动脚本版本为 `2.3.0`。
- [x] CLI 收敛为 `check-env` 与 `web`。
- [x] 删除离线任务、历史策略、上传、指标报告和旧页面代码。
- [x] 工具链收敛为 FFmpeg 与 FFprobe，环境检查只验证实时链路需要的能力。
- [x] 建立唯一 H.265 固定配置，并由命令与状态共同读取。
- [x] RTSP 探测只接受 H.264 视频。
- [x] 使用单一 RTSP 连接输出 H.264 elementary stream，并在一次计数后原样分发。
- [x] 源码 HLS 使用 `stream copy`，不使用预览编码器。
- [x] 原始帧只进入 H.265 编码器，H.265 字节在仅观看预览前统计。
- [x] 保留阻塞背压、512 MiB/约 5 秒队列、单会话、心跳和统一回收。
- [x] 增加源码长 GOP 启动告警和 RTSP 全字段脱敏。
- [x] 更新公开 API 与网页术语，负节省显示为码率增加。
- [x] 重建实时单元测试与真实 FFmpeg 合成流集成测试。
- [x] 将网页重构为视频优先的实时操作台，保留原视频画布并重排连接、控制、参数与遥测区域。
- [x] 固定参数卡片由 `/api/runtime` 的唯一 H.265 配置动态生成。
- [x] 对比分割线使用 Pointer Events、pointer capture 与动画帧提交；拖动只更新裁剪位置，不暂停视频，期间仅暂缓自动 seek 与调速。
- [x] 停止操作先立即卸载两路播放器，再等待后端统一回收进程，期间锁定会话控件并显示停止状态。
- [x] 后端在收到第一批 H.264 elementary stream 字节时建立 RTSP 实时基准，并在状态接口暴露 `rtsp_realtime_elapsed_seconds`。
- [x] 两路延迟标签改为当前播放画面相对 RTSP 实时基准的滞后秒数。
- [x] 前端删除共同时间轴、共同 seek 和软/硬同帧同步逻辑，并移除人为 10 秒目标延迟，两路各自贴近自身 HLS 最新安全播放边缘。
- [x] 任一路自动卡顿恢复仅作用于该路；手动播放/暂停、停止和页面关闭仍同时控制两路。
- [x] H.265 路因编码或预览转换落后时，夹到该路最新安全位置并显示真实更大的 RTSP 滞后。
- [x] 两路 HLS 保留窗口扩展为 60 个分片，播放列表禁止缓存和代理缓冲。
- [x] 页面租约放宽为 45 秒，心跳、状态查询和 HLS 文件请求均可续租。
- [x] 右路 H.264 仅观看预览调整为 `libx264 ultrafast / CRF 21`，正式 H.265 固定参数与码率统计保持不变。
- [x] 编码健康改为约 30 秒积压趋势，主状态显示“采样中 / 实时稳定 / 积压增长”，`encode_speed_x` 降为辅助处理节奏。
- [x] 右路 H.265 浏览器预览 HLS 降为 0.5 秒目标分片与 0.5 秒预览 GOP，正式 H.265 固定参数保持不变。
- [x] 前后端播放恢复安全缓冲统一降为 1 秒，并在 runtime、状态接口与页面参数卡片中展示。
- [x] 增加唯一固定轻度降噪配置 `hqdn3d=1.5:1.0:2.5:2.0`，位置固定在 H.264 解码后与 H.265 帧队列前。
- [x] 左路源码 H.264 继续原样计数与 stream copy，降噪只作用于右路且不增加新进程。
- [x] runtime 和流状态暴露 `denoise_config`，比较基准改为源码 H.264 与“轻度降噪 + H.265”完整方案。
- [x] `check-env` 增加 `hqdn3d` 能力验证，网页动态展示滤镜、强度和完整方案口径。

## 验证状态

- [x] 聚焦单元测试通过。
- [x] 合成 H.264 elementary stream 真实 FFmpeg 集成实验通过，两路 HLS 分片均可完整解码。
- [x] Python 全量编译检查通过。
- [x] 全部 22 项测试通过，并以 `ResourceWarning` 作为错误复核管道回收。
- [x] `check-env` 通过。
- [x] 前端 JavaScript 语法检查通过。
- [x] Playwright 桌面 1440x1000 与移动 390x844 页面检查通过；两路视频均完整加载并持续同步播放，无横向溢出、重叠或控制台错误。
- [x] Playwright 验证负节省显示为“码率增加”。
- [x] Playwright 在 1440x1000 与 390x844 复核新页面无横向溢出或控制台错误；分割线可稳定拖到 75.4% 并正确释放捕获。
- [x] Playwright 延迟后端停止响应时确认画面立即清空、状态切换为“停止中”且会话控件锁定。
- [x] Playwright 验证两路按 RTSP 实时基准独立显示滞后，源码和 H.265 画面时间允许不同。
- [x] Playwright 验证 H.265 单路卡顿不会自动暂停源码路，目标点超出该路 live edge 时夹到最新安全位置。
- [x] Playwright 验证固定积压显示“实时稳定”，持续约 `+0.6 s/min` 增长显示“积压增长”，`0.99x` 只作为辅助处理节奏。
- [x] 单元测试验证 runtime/status 中安全缓冲为 1 秒、右路预览分片为 0.5 秒，正式 `libx265` 参数未漂移。
- [x] Playwright 验证桌面与移动页面 runtime 显示安全缓冲 1 秒、右路预览 0.5 秒分片，且无横向溢出或控制台错误。
- [x] Playwright 最终复核 1440×1000 与 390×844 页面无横向溢出、文字裁切、元素重叠或控制台错误。
- [x] 确定性噪声灰场经真实 `hqdn3d` 后亮度噪声能量下降，输出帧尺寸、数量和 `yuv420p` 字节规模保持一致。
- [x] 1080p/25fps 正式 x265 参数三轮性能对照中，无滤镜与轻度降噪中位速度为 0.902x/0.901x，相对变化 -0.1%，通过 5% 回归门槛。
- [x] Playwright 验证 V2.3.0 桌面与移动页面显示唯一降噪配置，无横向溢出、重叠或控制台错误。
- [x] Playwright 注入播放状态后将分割线从 50% 拖到 75%，媒体 `pause/play` 调用均为 0，拖动状态正常释放且控制台无错误。
- [x] 全仓已删除能力与旧接口残留扫描通过。
- [x] 本次真实集成测试创建的 FFmpeg 与管道全部回收；未操作系统中既有的 8000 端口旧服务进程。

## 运行边界

- 当前没有真实 RTSP 凭据，因此合成流集成实验替代外部摄像头连续运行检查。
- `work/` 中已有用户结果未删除，V2.3.0 不读取这些旧结果。
