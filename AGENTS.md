# Codex 项目协作规则

## 开始工作前

每个新对话在分析、设计或修改代码前，必须依次完整阅读：

1. `PROJECT_CONTEXT.md`
2. `DESIGN_V1.md`
3. `TASKS.md`
4. `README.md`

如果代码现状与文档冲突，以 `DESIGN_V1.md` 的当前有效方案和 `TASKS.md` 的任务状态为准，并先说明冲突。

## 当前唯一方向

- 当前课题是“H.265 默认编码与三档综合策略研究工具 V1.0”。
- 当前正式入口独立生成 x265 默认、保守综合、均衡综合和激进综合四个视频。
- 三档综合策略分别搜索自己的绝对画质合格 CRF，不做 CRF 配对，不评选胜出方案。
- 旧版网页、RTSP、MediaMTX、实时动态码率和手工目标码率比例设计均已废弃，不得自行恢复。
- `DESIGN_V1.md` 第0节是 v0.11.0 当前唯一有效方案；后续旧章节仅为历史记录。

## 结论边界

- 码率节省百分比只表示三档综合输出相对 x265 默认输出的包码率变化，不是胜出或部署结论。
- 软件 x265 实验不能表述为摄像头硬件编码器已经验证成功。
- 上传的 MP4/MKV 是压缩后的输入参考视频，不得称为传感器原始帧。
- 负节省必须保留并表述为码率增加，不得截断或隐藏。
- 报告不得评选最佳方案，也不得生成摄像头部署结论。

## 开发约束

- 默认使用中文说明、注释和报告，代码标识符使用英文。
- 使用 Python 3.9；尽量保持 Python 标准库实现，视频处理使用项目内 `.tools` 中的 FFmpeg/FFprobe/VMAF。
- 不允许通过手工目标码率伪造结果；三档 CRF 必须由各自绝对画质门槛搜索得到。
- 所有候选必须保持输入参考画面的分辨率、帧率、帧数、时长和 Main 8-bit 输出要求。
- 视频码率按视频流包字节计算；音频和容器开销不得混入核心视频码率。
- 中间结果应缓存，程序中断后可以继续。
- 修改完成后运行单元测试、环境检查和至少一次端到端实验。
- 完成任务后更新 `TASKS.md`；形成长期有效的结论时同步更新 `PROJECT_CONTEXT.md`，不要记录密码、令牌、私有 RTSP 地址或公司敏感信息。

## 新对话建议提示词

```text
请先完整阅读 AGENTS.md、PROJECT_CONTEXT.md、DESIGN_V1.md、TASKS.md 和 README.md。
以 DESIGN_V1.md 为当前唯一方案，检查代码现状，然后从 TASKS.md 第一项未完成任务继续。
不要恢复旧版网页、RTSP 或动态码率架构。
```
