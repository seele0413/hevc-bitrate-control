# Codex 项目协作规则

## 开始工作前

每个新对话在分析、设计或修改代码前，必须依次完整阅读：

1. `PROJECT_CONTEXT.md`
2. `DESIGN_V1.md`
3. `TASKS.md`
4. `README.md`

如果代码现状与文档冲突，以 `DESIGN_V1.md` 的当前有效方案和 `TASKS.md` 的任务状态为准，并先说明冲突。

## 当前唯一方向

- 当前课题是“H.265 默认编码与 V1.4 预算中性 ROI 策略研究工具”。
- 当前正式入口独立生成 x265 默认、通用无 ROI、预算中性 ROI、ROI + 降噪实验项四个视频或失败记录。
- 默认方案为 x265 原生默认编码，只指定 `libx265`，不传自定义 CRF、preset、AQ、ROI、降噪或帧间参数。
- 通用无 ROI 方案是当前 ROI 预算基准：复用 V1.0 原激进帧间结构，preset 为 `medium`，启用 AQ2，但不使用静态 ROI QP 和 ROI 分区降噪。
- 预算中性 ROI 方案在通用无 ROI 方案的平均视频包码率预算内，仅使用静态 ROI QP 重新分配码率。
- ROI + 降噪实验项在同一预算内叠加静态 ROI QP 和 ROI 保护分区降噪；它是实验项，不得默认表述为收益。
- 三个非默认策略统一使用 VMAF ≥ 83、P5 ≥ 80、SSIM ≥ 0.950；各自独立搜索 CRF，不做 CRF 配对，不评选胜出方案，也不生成部署结论。
- ROI 候选还必须满足平均视频包码率不超过通用无 ROI 方案，并且 critical/evidence 重点区域局部 VMAF、P5、SSIM 不低于通用无 ROI 方案；任一条件失败即标记 ROI 失败，不发布对应 ROI 输出。
- 旧版网页、RTSP、MediaMTX、实时动态码率和手工目标码率比例设计均已废弃，不得自行恢复。
- `DESIGN_V1.md` 第0节是 v1.4.0 当前唯一有效方案；后续旧章节仅为历史记录。

## 结论边界

- 码率节省百分比必须同时区分相对 x265 默认和相对通用无 ROI 方案；负节省必须保留，不是胜出或部署结论。
- ROI 是否预算中性、ROI 是否真的改善重点区域必须在报告中明确列出。
- 软件 x265 实验不能表述为摄像头硬件编码器已经验证成功。
- 上传的 MP4/MKV 是压缩后的输入参考视频，不得称为传感器原始帧。
- 负节省必须保留并表述为码率增加，不得截断或隐藏。
- 报告不得评选最佳方案，也不得生成摄像头部署结论。

## 开发约束

- 默认使用中文说明、注释和报告，代码标识符使用英文。
- 使用 Python 3.9；尽量保持 Python 标准库实现，视频处理使用项目内 `.tools` 中的 FFmpeg/FFprobe/VMAF。
- 不允许通过手工目标码率伪造结果；通用、ROI、ROI + 降噪三档 CRF 必须由各自绝对画质门槛搜索得到。
- 所有候选必须保持输入参考画面的分辨率、帧率、帧数、时长和 Main 8-bit 输出要求。
- 视频码率按视频流包字节计算；音频和容器开销不得混入核心视频码率。
- 中间结果应缓存，程序中断后可以继续。
- 修改完成后运行单元测试、环境检查和至少一次端到端实验。
- 完成任务后更新 `TASKS.md`；形成长期有效的结论时同步更新 `PROJECT_CONTEXT.md`，不要记录密码、令牌、私有 RTSP 地址或公司敏感信息。

## 新对话建议提示词

```text
请先完整阅读 AGENTS.md、PROJECT_CONTEXT.md、DESIGN_V1.md、TASKS.md 和 README.md。
以 DESIGN_V1.md 为当前唯一方案，检查代码现状，然后从 TASKS.md 第一项未完成任务继续。
不要恢复旧版网页、RTSP 或动态码率架构。
```
