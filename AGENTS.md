# Codex 项目协作规则

## 开始工作前

每个新对话在分析、设计或修改代码前，必须依次完整阅读：

1. `PROJECT_CONTEXT.md`
2. `DESIGN_V1.md`
3. `TASKS.md`
4. `README.md`

如果代码现状与文档冲突，以 `DESIGN_V1.md` 的当前方案和 `TASKS.md` 的任务状态为准，并先说明冲突。

## 当前唯一方向

- 当前项目是 V2.2.1 Remote Stable H.264 源码直流与 H.265 固定参数实时工具。
- 只接受 H.264 RTSP 视频，不处理音频，也不对其他输入编码做隐式转换。
- 左路复用同一份 H.264 elementary stream 字节，以 `stream copy` 重封装为 HLS。
- 右路把同一源流解码为 `yuv420p`，通过连续性优先有界队列送入唯一 H.265 固定参数编码器。
- H.265 字节在浏览器预览转换前统计；其后的限码 H.264 HLS 只供观看，不参与码率比较。
- 右路浏览器预览固定使用 `libx264 ultrafast / CRF 26 / maxrate 3M / bufsize 6M / 1 秒 GOP / 1 秒 HLS`，保持源分辨率。
- 浏览器两路独立播放，不强制同帧：源码目标 HLS 边缘延迟 10 秒，H.265 预览目标 15 秒。
- 每一路以自身 `video.buffered` 判断恢复：低于 1.5 秒只暂停该路，在当前固定延迟目标重新获得至少 8 秒实际缓冲后恢复。
- 手动播放、暂停、停止、页面关闭仍同时控制两路；拖动分割线期间仍冻结双路画面。
- 项目只保留 `check-env` 与 `web` 两个 CLI 命令，不恢复离线任务、旧页面、降噪或历史策略。

## 结论边界

- 码率节省只按两路 elementary stream 字节的最近 30 秒滚动窗口计算。
- `bandwidth_saving_pct` 为负时原样返回并显示为码率增加。
- HLS 实际传输码率按当前播放列表最新约 30 秒已封口且存在的 TS 分片统计，与码率节省证据分开。
- 浏览器下载速度、实际缓冲、带宽余量和卡顿次数只属于当前浏览器会话，不写回后端。
- 页面延迟是相对各自 HLS live edge 的延迟，不是摄像头曝光时间或 RTSP 绝对墙钟延迟。
- 本机软件编码结果不得表述为摄像头硬件编码器验证结果。
- 源码直通是 H.264 压缩码流不重新编码，不得称为传感器原始帧。
- H.265 后续的 H.264 转换仅供浏览器观看，不是比较基准。

## 开发约束

- 默认使用中文说明、注释和界面，代码标识符使用英文。
- 使用 Python 3.9，视频处理使用项目内 `.tools/ffmpeg` 的 FFmpeg 与 FFprobe。
- H.265 与浏览器预览参数只能来自 `hevc_lab/config.py` 的唯一配置。
- 正式 H.265 固定参数和 elementary stream 计数口径不得因预览优化而改变。
- 源码字节必须先计数一次，再把完全相同的字节送入 HLS 重封装和解码两个分支。
- 队列满时阻塞上游，不主动丢帧；容量约 5 秒且不超过 512 MiB。
- 保持单会话、45 秒页面租约和统一子进程回收；任一分支退出必须停止整组进程。
- HLS 保留 60 个分片；源码 HLS 不重新编码修正关键帧，右路预览目标为 1 秒分片。
- 播放列表禁止缓存和代理缓冲；状态或 HLS 请求均可续租。
- HLS 文件响应必须先读取为不可变字节快照，避免文件轮转或停止清理导致长度竞态。
- 回收管线时必须先 terminate/wait/kill 子进程，再关闭带缓冲的 stdin，禁止因 flush 阻塞停止。
- RTSP 日志和状态必须删除用户名、密码、完整路径、query 与 fragment。
- 不删除 `work/` 中已有用户结果；当前独立副本不含原仓库历史 `work/`。
- 第一阶段不修正声明帧率与实际帧率差异，不重构共享阻塞背压管线。
- 修改完成后运行编译检查、全部单元测试、环境检查、真实 FFmpeg 集成实验和浏览器桌面/移动验证。
- 完成任务后更新 `TASKS.md`；形成长期事实时同步更新 `PROJECT_CONTEXT.md`。
