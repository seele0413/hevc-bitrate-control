# Codex 项目协作规则

## 开始工作前

每个新对话在分析、设计或修改代码前，必须依次完整阅读：

1. `PROJECT_CONTEXT.md`
2. `DESIGN_V1.md`
3. `TASKS.md`
4. `README.md`

如果代码现状与文档冲突，以 `DESIGN_V1.md` 的当前方案和 `TASKS.md` 的任务状态为准，并先说明冲突。

## 当前唯一方向

- 当前项目是 V2.3.0 H.264 源码直流与固定轻度降噪 H.265 实时工具。
- 只接受 H.264 RTSP 视频，不处理音频，也不对其他输入编码做隐式转换。
- 左路复用同一份 H.264 elementary stream 字节，以 `stream copy` 重封装为 HLS。
- 右路把同一源流解码后以固定 `hqdn3d=1.5:1.0:2.5:2.0` 轻度降噪，再输出 `yuv420p`，通过连续性优先有界队列送入唯一 H.265 固定参数编码器。
- H.265 字节在任何预览转换之前统计；其后生成的 H.264 HLS 只供浏览器观看，不参与码率计算。
- 项目只保留 `check-env` 与 `web` 两个 CLI 命令，不恢复已删除的离线任务、旧页面或旧策略。
- 浏览器采用 RTSP 实时基准滞后显示：后端以第一批源码字节建立 `rtsp_realtime_elapsed_seconds`，两路不设人为目标延迟，各自贴近自身 HLS 最新安全播放边缘，安全缓冲为 1 秒，不再强制同帧；任一路自动卡顿恢复只作用于该路，手动播放/暂停和停止仍同时控制两路，拖动分割线只改变裁剪位置，不暂停视频。
- `DESIGN_V1.md` 全文只描述 V2.3.0 当前方案，不再保留历史章节。

## 结论边界

- 码率只按两路 elementary stream 字节的最近 30 秒滚动窗口计算。
- `bandwidth_saving_pct` 为负时必须原样返回并显示为码率增加。
- RTSP 基准播放滞后与 H.265 编码积压必须分别表达。
- 本机软件编码结果不得表述为摄像头硬件编码器验证结果。
- 源码直通是 H.264 压缩码流不重新编码，不得称为传感器原始帧。
- H.265 后续的 H.264 转换仅供浏览器观看，不是比较基准。
- 码率节省是源码 H.264 与“轻度降噪 + H.265”完整方案的比较，不得表述为纯编码格式收益。

## 开发约束

- 默认使用中文说明、注释和界面，代码标识符使用英文。
- 使用 Python 3.9，视频处理使用项目内 `.tools/ffmpeg` 的 FFmpeg 与 FFprobe。
- H.265 与轻度降噪参数只能来自 `hevc_lab/config.py` 的唯一配置，命令、状态与网页不得各自维护副本。
- 源码字节必须先计数一次，再把完全相同的字节送入 HLS 重封装和解码两个分支。
- 队列满时阻塞上游，不主动丢帧；容量约 5 秒且不超过 512 MiB。
- 保持单会话、页面心跳租约和统一子进程回收；任一分支退出必须停止整组进程。
- HLS 保留 60 个分片，源码 HLS 不重新编码修正关键帧，右路仅观看预览 HLS 目标分片为 0.5 秒；播放列表禁止缓存和代理缓冲；状态或 HLS 请求均可续租，租约超时为 45 秒。
- RTSP 日志和状态必须删除用户名、密码、完整路径、query 与 fragment。
- 不删除 `work/` 中已有用户结果；新程序不读取其中的离线结果。
- 修改完成后运行编译检查、全部单元测试、环境检查、真实 FFmpeg 集成实验和浏览器桌面/移动验证。
- 完成任务后更新 `TASKS.md`；形成长期事实时同步更新 `PROJECT_CONTEXT.md`。
